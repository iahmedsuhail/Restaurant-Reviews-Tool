import React from 'react';

const NavigationBar = () => {
    return(
        <div style={{display: 'flex', justifyContent: 'flex-end'}}>
            <p className='f3 link dim black underline pa3 pointer'> something for navigation</p>
        </div>
    );
}
export default NavigationBar;